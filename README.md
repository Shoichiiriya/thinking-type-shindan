# 思考法タイプ診断

性格ではなく「考えるときのクセ」を測る36問の診断。
MBTI互換の4文字コード（`CALR-O` 形式）を返す。

→ https://shoichiiriya.github.io/thinking-type-shindan/

- 静的HTML 1枚。サーバー・ログイン・保存なし
- 回答はどこにも送信されない（結果はURLパラメータに載るだけ）

このリポジトリは公開用。`index.html` は開発リポジトリから配置している。
